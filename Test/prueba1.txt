program pruebaNueva;
vars
    int: i, j, k[100];
    float: p, q;
function void idFunction(int: f, z)
vars
    int: h;
    float: m, n;
    char: r[5];
{
    h = i + 1;
    m = n + m;
}
main() {
    regression(k, k);
    idFunction(i, j);
}